{
  "name": "web-boteca",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "npx playwright test --config=playwright.config.cjs",
    "test:desktop": "npx playwright test --config=playwright.config.cjs --project=desktop",
    "test:mobile": "npx playwright test --config=playwright.config.cjs --project=mobile"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.15",
    "@astrojs/sitemap": "^3.7.0",
    "astro": "^5.18.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "netlify-cli": "^24.0.0"
  }
}
